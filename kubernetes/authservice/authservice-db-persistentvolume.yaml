apiVersion: v1
kind: PersistentVolume
metadata:
  name: authservice-db-data
  namespace: whatsdown
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"