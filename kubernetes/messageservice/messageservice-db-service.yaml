apiVersion: v1
kind: Service
metadata:
  labels:
    whatsdown.service: messageservice-db
  name: messageservice-db
  namespace: whatsdown
spec:
  type: ClusterIP
  ports:
  - name: "messageservice-db"
    protocol: TCP
    port: 3308
    targetPort: 3308
  selector:
    whatsdown.service: messageservice-db