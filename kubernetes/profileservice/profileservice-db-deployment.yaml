apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    whatsdown.service: profileservice-db
    app: profileservice
    version: "1.0"
  name: profileservice-db
  namespace: whatsdown
spec:
  replicas: 1
  selector:
    matchLabels:
      whatsdown.service: profileservice-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        whatsdown.service: profileservice-db
        app: profileservice
        version: "1.0"
    spec:
      containers:
      - image: mysql:8.0.24
        imagePullPolicy: IfNotPresent
        args:
        - --default-authentication-plugin=mysql_native_password
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: password
        name: profileservice-db
        ports:
        - containerPort: 3309
        
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "100m"
        volumeMounts:
        - mountPath: /var/lib/mysql
          name: profileservice-persistent-storage
      hostname: profileservice-db
      restartPolicy: Always
      volumes:
      - name: profileservice-persistent-storage
        persistentVolumeClaim:
          claimName: profileservice-pv-claim